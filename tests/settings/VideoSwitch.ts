// Flip by hand before/after a deliberate promoVideo recording session, then flip back to false -
// see tests/e2e/fixtures/playwright.ts for why this can't be gated dynamically instead.
export const RECORD_VIDEO = true;
