# Fobles Project TODO

## TODO - V0

- [ ] Document the rationale for broad host permissions in the store submission: Fobles supports arbitrary Sitecore hosts and applies Sitecore path eligibility checks at runtime.
- [ ] Add a release packaging command that creates a store-ready archive from `dist/unpacked/` into `dist/packed/`.
- [ ] Define the release process for version updates, Git tags, and GitHub Releases.
- [ ] Decide how unpacked development settings should migrate to the eventual store extension ID.
- [ ] Investigate adding ESLint (with a TypeScript config) to the project - no linter currently exists (no `lint` script, no ESLint devDependency). Would let `.github/workflows/ci.yml` require lint alongside typecheck/build.
- [ ] File/Image field strategies (`sc-file.ts`/`sc-image.ts`) call `ensurePathShape(value, "media")` on `input.value.trim()` to compute the Fobles button's navigation target, where `value` is read directly from the DOM input. It's unconfirmed whether Sitecore's File/Image editor controls render that input's live value as the raw stored XML (e.g. `<file mediaid="{GUID}" src="..." />`, the field's serialized value) or as an already-resolved media path (matching the "client control resolves the value before Fobles reads it" pattern already confirmed for General Link/Drop Tree) - needs a live DOM check (inspect the input's actual `.value` in a real Content Editor session) before `tests/e2e/strategies/file.spec.ts`/`image.spec.ts` can hardcode a correct expected navigation value; guessing risks a nonsensical assertion (`ensurePathShape` applied to raw XML wouldn't produce a sane path).
- [ ] The "Package Designer" quick-menu button's jump target is incorrect (confirmed by the user via a live run) - needs investigation into what URL/hdl value it should actually use.
- [ ] Track every `default.aspx?xmlcontrol=...` dialog page (rendered only inside an iframe, not a full Content Editor page) that Fobles should show its toolbar/nav on, and add an e2e test per page confirming the nav actually appears (none exist yet). Known so far, all already in `FOBLES_PAGES` (`src/content/constants.ts`):
  - `FileExplorer` - reached via the "File Explorer" quick-menu button
  - `Sitecore.Shell.Applications.Dialogs.SelectRendering` - compact toolbar (`toolbarType: "compact"`)
  - `AddFromTemplate`
  - `Gallery.Subitems` - compact toolbar; reached via Navigate ribbon tab -> Subitems (needs a macro, like `openLinksGallery`/`sitecore-macros.ts`)
  - `Gallery.Favorites` - compact toolbar; reached via Navigate ribbon tab -> Favorites
  - `TreeListExEditor` - compact toolbar; reached via Configure ribbon tab -> Editors
  - `DeviceEditor` - compact toolbar; reached via Presentation ribbon tab -> Details
- [ ] The "other menu buttons navigate to their configured URLs" test's (`tests/e2e/toolbar/fobles.spec.ts`) step screenshot for "Show Config" (`/sitecore/admin/showconfig.aspx`) captures the wrong page - confirmed live. showconfig.aspx is already known to be slow to render (dumps the whole live web.config into an XML tree viewer - see `STEP_TIMEOUT_MS`'s comment in `tests/e2e/CONST.ts`), so `page.waitForLoadState("load")` right before the screenshot likely resolves before the page has actually painted its real content. Needs investigation into a more reliable "actually rendered" wait for this specific target before the auto step-screenshot fires.
- [ ] Finish the promo video: run the full walkthrough, save it via `npm run test:e2e:promoVideo:save`, then flip `RECORD_VIDEO` back to `false` in `tests/settings/VideoSwitch.ts`.

## TODO - V1

- [ ] Finalize and publish the project license and contribution terms. Until then, `LICENSE` is an all-rights-reserved notice and `LICENSE-DRAFT.md` is non-binding.
- [ ] Investigate the circular import between `src/content/toolbar/quick-menu` and `src/content/toolbar/proxy-buttons.ts` (`quick-menu` imports `setProxyButtonsVisible` from `proxy-buttons.ts`, which imports `setQuickMenuVisible` from `quick-menu`). Works today but is fragile under refactors and blocks tree-shaking/isolated testing.
- [ ] `src/content/features/quick-menu/kick-users.ts` and `ai-pages.ts` are organizational anomalies: they're non-UI command/action logic that doesn't fit `toolbar/` (rendering-only) or `features/augmentor/` (Sitecore-page-manipulation commands) cleanly. Likely belong in `augmentor/` once revisited, since they do manipulate the real Sitecore page (kicking users, reading item info) - just not yet moved there.
- [ ] `Strategy General Link Anchor` field (tests/items-fobles, `/sitecore/system/Modules/Fobles Testing/Strategy Scenarios/Strategy general link`) has no value yet - `Internal`/`External`/`Media`/`Email`/`JavaScript` are all confirmed real values now. Set it via Content Editor's real "Insert Anchor" dialog and `ser pull`.
- [ ] `Strategy Image Content Hub` field has no value yet - waiting on a real Content Hub link to test against.
- [ ] Strategy Tag List field doesn't render the real Tag List widget - falls back to a plain pipe-delimited-GUID text box instead of the two-pane tree+select UI `sc-taglist.ts` expects (`td[rowspan] > .scScrollbox` + `select.scContentControlMultilistBox`), so `tests/e2e/strategies/tag-list.spec.ts` is temporarily skipped (`test.describe.skip`). Likely cause: the field's datasource items (`Fobles Data Item`, shared with other strategy tests) only inherit Sitecore's generic Standard template, not whatever "Tagging" section template real Tag items need (possibly `/sitecore/templates/System/Templates/Sections/Tagging`, unverified GUID `{0AA6D3F8-C9D0-401F-83AA-A41433C24767}`). Needs: confirm that template exists/its real GUID in this instance, then either add it as a base template to the shared test data or give Tag List its own dedicated datasource, and un-skip the test.
  - Update: the "Strategy Tag List 0x/1x/3x/10x" template fields' `Type` value was also found to be the invalid string `"Tag List"` (Sitecore silently renders a blank Type dropdown for a value it doesn't recognize) - fixed to the correct `"Taglist"` and pulled. Even accounting for that, still suspect something more fundamentally wrong with how this Sitecore instance's Taglist field type behaves - worth deeper investigation before assuming the datasource-template theory above is the whole story.
- [ ] `applyIconStrategy` (`src/content/features/augmentor/field-strategies/sc-icon.ts`) only sets the Fobles marker attribute on Icon inputs - it never calls `createFoblesItemButton`/creates a wrapper, unlike every other single-input strategy (File/Image use `applySingleInputFieldStrategy`, which Icon doesn't). No Fobles button ever appears on an Icon field. Unclear whether this is an intentional stub or a real bug - needs a decision (wire it through `applySingleInputFieldStrategy` like File/Image, or explicitly document Icon as unsupported) before `tests/e2e/strategies/icon.spec.ts` can be written as a real click-navigation test.
- [ ] The "PowerShell ISE" quick-menu button (`useCurrentItemId: true`) currently opens ISE against whatever item happens to be current, rather than a real PowerShell Script Library item - for a more meaningful test/screenshot it should navigate to a real built-in SPE script first (e.g. `{56DA8E7D-6CA5-4576-B1ED-4EDCCCC5B0B4}` / `/sitecore/system/Modules/PowerShell/Script Library/SPE/Core/Platform/Functions/BaseXlsx`), but that requires the Sitecore PowerShell Extensions (SPE) module to be installed, which isn't guaranteed in every environment - deferred until it actually comes up.
- [ ] Turn `scTileItem` elements into Fobles buttons - found on CE -> Folder (a Folder-templated item's content area renders its children as tiles instead of the usual field/list layout). Each tile is an `<a class="scTileItem" onclick="...item:load(id={GUID})...">` wrapping an icon + display name, e.g.:
  ```html
  <a id="I{C42B5D47-E770-449C-88D0-DE7EEFD6A6B6}" href="#" class="scTileItem" onclick="javascript:scForm.getParentForm().invoke('item:load(id={C42B5D47-E770-449C-88D0-DE7EEFD6A6B6})');return false"><div class="scTileItemPane"><div class="scTileItemIcon"><img src="/temp/iconcache/applications/32x32/folder.png" class="scTileItemIconImage" alt="" border="0"></div><div class="scTileItemDetails"><div class="scTileItemDetailsHeader">Dynamic Placeholders</div></div></div></a>
  ```
- [ ] (maybe) Add Fobles buttons to values in a template field's "Source" field - needs investigation into what the rendered markup/value actually looks like before committing to an approach.
- [ ] New strategy needed: the `Gallery.Subitems` dialog (already tracked above under `FOBLES_PAGES`) lists each subitem as a `<tr id="M...">` row with an `onclick="...item:load(id={GUID},language=en,version=N)..."` handler and a plain-text `.scMenuItemCaption` cell - no Fobles button renders on these today. Likely the same shape applies to `Gallery.Favorites` (same underlying gallery menu widget). Example row:
  ```html
  <tr id="M7214591" onmouseover="javascript:return scForm.rollOver(this,event)" onfocus="javascript:return scForm.rollOver(this,event)" onmouseout="javascript:return scForm.rollOver(this,event)" onblur="javascript:return scForm.rollOver(this,event)" onclick="javascript:return scForm.postEvent(this,event,'item:load(id={389E8D4C-5E52-45BA-A4D9-F0BAB18B0B86},language=en,version=2)')"><td class="scMenuItemIcon"><img src="/temp/iconcache/business/16x16/note.png" width="16" height="16" align="middle" class="" alt="" border="0"></td><td class="scMenuItemCaption">Grow Your Business</td><td class="scMenuItemHotkey"><img src="/sitecore/images/blank.gif" width="1" height="1" class="scSpacer" alt="" border="0"></td></tr>
  ```
- [ ] The Fobles toolbar doesn't actually appear on `Gallery.Subitems` or `Gallery.Favorites` (confirmed failing live - `tests/e2e/editor/toolbar-visibility.spec.ts`'s "Gallery Subitems"/"Gallery Favorites" cases are temporarily `skip: true`) even though both are listed in `FOBLES_PAGES` as eligible - needs investigation into why (possibly related to the toolbar-position-sync bug below, possibly unrelated) before un-skipping. Likely the same root cause given both are the same underlying gallery menu widget (see the `scMenuItemCaption` new-strategy note above).
- [ ] New strategy needed: a rendering's "Data Source" field (Presentation Details -> Controls -> Edit) isn't picked up by any existing field-strategy - it renders as a plain `input.scContentControl` holding a raw content path (not XML, not a media path), but its action buttons use `contentrenderingdatasource:browse/clear` and `contentExtension:buildquery/pastequery`, prefixes none of `ACTION_PREFIXES` (`src/content/sitecore.ts`) currently matches, so `hasActionPrefix` (`shared/apply-single-input-field.ts`) always rejects it. Needs a decision on whether hiding the whole `.scContentButtons` bar (existing convention for File/General Link/Internal Link) is still right here, since Build Query/Paste Query aren't navigation actions like Browse/Clear are. Example markup:
  ```html
  <td class="scEditorFieldMarkerInputCell"><div class="scEditorFieldLabel">Data Source<span class="scEditorFieldLabelAdministrator"> [shared]</span>:</div><div class="scContentButtons"><a href="#" class="scContentButton" onclick="javascript:return scForm.postEvent(this,event,'contentrenderingdatasource:browse(id=FIELD7215184)')">Browse</a><a href="#" class="scContentButton" onclick="javascript:return scForm.postEvent(this,event,'contentExtension:buildquery(id=FIELD7215184)')">Build query</a><a href="#" class="scContentButton" onclick="javascript:return scForm.postEvent(this,event,'contentExtension:pastequery(id=FIELD7215184)')">Paste query</a><a href="#" class="scContentButton" onclick="javascript:return scForm.postEvent(this,event,'contentrenderingdatasource:clear(id=FIELD7215184)')">Clear</a></div><div style=""><input id="FIELD7215184" class="scContentControl" value="/sitecore/content/Home/Build A Page Content Bits/Information Boxes/Kestra Independence - Business Consulting" onfocus="javascript:return scContent.activate(this,event,'sitecore://master/{BBF43F29-E42C-4B87-9401-945F6D807C4C}?lang=en&amp;ver=1&amp;fld={0793DCFF-0EE7-48AD-B9D4-EF1DD24EB59B}&amp;ctl=FIELD7215184')" oninput="javascript:if (window.top.document.getElementById('scLayoutDefinition')){window.top.document.getElementById('scLayoutDefinition').setAttribute('datasourceReload','true');}" onblur="javascript:return scContent.deactivate(this,event,'sitecore://master/{BBF43F29-E42C-4B87-9401-945F6D807C4C}?lang=en&amp;ver=1&amp;fld={0793DCFF-0EE7-48AD-B9D4-EF1DD24EB59B}&amp;ctl=FIELD7215184')" placeholder=""></div></td>
  ```

## Resolved

- [x] Toolbar drag position saved per-page - re-verified live (manual test) that it persists correctly now via `toolbar-placement.ts`'s `foblesNavPosition_<pageId>` sync-key design (`setPlacementForPage`/`currentAllowedPage` in `src/content/toolbar-runtime.ts`). Likely fixed by the `FOBLES_PAGES`/per-`pageId` refactor - this note was stale.
- [x] `toggleTreeButtons` in `src/content/features/augmentor/treeNodeFobles/index.ts` nested 6 function declarations (~150 lines) inside one outer function - hoisted `buildTreeButtonUrl`, `getTreeNodeItemId`, `addTreeOpenButton`, and `walkTreeForButtons` to module level so the dispatch logic isn't buried after the helpers.

## Known Limitations

- Authenticated Sitecore end-to-end tests require a configured private environment and interactive login. They are not run by ordinary hosted CI.
- `dist/unpacked/` and `tests/test-artifacts/` are generated local output and are intentionally ignored by Git.
